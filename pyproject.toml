[project]
name = "docuquery"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Harry", email = "hareom284@gmail.com" }
]
requires-python = ">=3.12"
dependencies = [
    "httpx>=0.28.1",
]

[project.scripts]
docuquery = "docuquery:main"

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"

[dependency-groups]
dev = [
    "pytest>=9.1.1",
    "ruff>=0.16.6",
]
